@model LogisticsApp.Models.Truck

@{
    ViewData["Title"] = "Подтверждение удаления грузовика";
}

<h1>Подтвердите удаление грузовика</h1>

<p>Вы уверены, что хотите удалить грузовик "<strong>@Model.Brand @Model.Model @Model.StateNumber</strong>"?</p>

<form asp-action="DeleteConfirmed" asp-route-id="@Model.Id" method="post">
    <button type="submit" class="btn btn-danger">Удалить</button>
    <a href="@Url.Action("Index", "Truck")" class="btn btn-secondary">Отмена</a>
</form>
